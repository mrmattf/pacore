# Database
DATABASE_URL=postgresql://postgres:password@localhost:5432/pacore

# Redis
REDIS_URL=redis://localhost:6379

# Vector Store Configuration
# Options: 'pgvector' (default, uses PostgreSQL) or 'pinecone' (requires API key)
VECTOR_STORE=pgvector

# Pinecone Vector Database (only needed if VECTOR_STORE=pinecone)
PINECONE_API_KEY=your-pinecone-api-key
PINECONE_INDEX_NAME=pacore-conversations

# Server
PORT=3000
NODE_ENV=development

# Security
JWT_SECRET=your-very-secure-secret-key-change-in-production

# CORS
CORS_ORIGINS=http://localhost:3001,http://localhost:3000

# Default LLM Provider (optional)
DEFAULT_PROVIDER=anthropic

# Platform-hosted MCP Servers (optional — omit to disable)
# Set PLATFORM_MCP_SHOPIFY_GORGIAS_URL to the URL of your hosted shopify-backorder service.
# Set PLATFORM_MCP_SHOPIFY_GORGIAS_SECRET to the API_SECRET configured in that service.
PLATFORM_MCP_SHOPIFY_GORGIAS_URL=http://localhost:3002
PLATFORM_MCP_SHOPIFY_GORGIAS_SECRET=your-shopify-backorder-api-secret
