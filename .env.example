# Database
DB_PASSWORD=pacore123
DATABASE_URL=postgresql://pacore:pacore123@localhost:5432/pacore

# Redis
REDIS_URL=redis://localhost:6379

# Vector Store Configuration
# Options: 'pgvector' (default, uses PostgreSQL) or 'pinecone' (requires API key)
VECTOR_STORE=pgvector

# Pinecone Vector Database (only needed if VECTOR_STORE=pinecone)
PINECONE_API_KEY=your-pinecone-api-key-here
PINECONE_INDEX_NAME=pacore-conversations

# JWT Secret
JWT_SECRET=your-very-secure-secret-key-change-in-production

# CORS
CORS_ORIGINS=http://localhost:3001,http://localhost:3000

# Node Environment
NODE_ENV=development
PORT=3000

# Agent Configuration (for on-premise agent)
AGENT_TOKEN=your-agent-token-here
CLOUD_URL=http://localhost:3000
AGENT_ID=agent-1

# Ollama (for local LLM)
OLLAMA_HOST=http://localhost:11434
